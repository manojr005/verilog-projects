module full_adder(a,b,ci,sum,co);
	input a,b,cin;
	output sum,co;
	wire s1,c1,c2;
	
	half h1(a,b,s1,c1);
	half h2(s1,cin,sum,c2);	
	or a1(co,c1,c2);	

endmodule

module half(a,b,sum,carry);
	input a,b;
	output sum,carry;
	 
	assign sum = a^b;
	assign carry = a&b;

endmodule